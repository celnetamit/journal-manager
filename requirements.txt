# Pinned to exact versions verified to install and run on Python 3.12.
# Pins keep Docker/Coolify builds reproducible and cacheable, and stop
# unattended deploys from pulling brand-new major releases (e.g. pandas 3,
# numpy 2.4, protobuf 7) that can break the build or runtime.
# To refresh: bump a pin, rebuild, smoke-test, then commit.
streamlit==1.58.0
python-docx==1.2.0
numpy==2.4.6
pandas==3.0.3
requests==2.34.2
google-genai==2.8.0
bcrypt==5.0.0
psycopg[binary,pool]==3.3.4
streamlit-cookies-manager-v2==0.3.1
# cryptography (Fernet) + its cffi backend back the encrypted "Remember me"
# cookie. They arrive transitively via streamlit-cookies-manager-v2, but pin
# them explicitly so a runtime missing cffi's native backend can't silently
# disable persistent cookies. Mature majors, not bleeding edge.
cryptography==44.0.1
cffi==1.17.1
Authlib==1.3.2
